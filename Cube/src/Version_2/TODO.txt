
        DIVIDE AND CONQUER AT ALL TIMES


Todo DONEs:
     DONE implement the interface onto the cube
     DONE List for all objects in the world
     DONE outsource the projection and drawing method to the objects
     DONE Add move methods to object for X/Y/Z axes
     DONE Add move method(s) to object for moving by vector
     DONE Add Vector class
     DONE only draw the needed lines
     DONE Order of Methods in Object / Cube
     DONE Multi-object render (ignoring visibility & clipping, for now) -> Project-all Method for world, so that after changes to one object all the others get redone

     ON HOLD make setters private if not needed publicly, reduce work later

Todo :
     finish ReadMe (for now 15/04/26)
     add ifs for setters -> trust that the pre-conditions are uphold?
     Animations-Class allowing animtions or changes unversal for all objects
     Give the Camera a Coordination
     Add VOW
     Add Rotation methods to all objects
     Add Camera
     Make the camera movable
     make the camera rotatable
     what happens if the object is behind the camera -> dividing by 0
     change the calculation to check if the object is in front of the camera
     what if objects are partly infront of the camera but the rest is behind it, how to draw the lines to the back -> Filter into an Array of visible Objects -> filter that into array of visible faces (maybe)
     Save frames to a png/jpeg
     how to manipulate picture saving to make parts transparent, so it only shows the objects
     Add a length to the vector to not need to get the length of it out of the direction



     Current: finish ReadMe (for now 15/04/26)